services:
  jenkins:
    build:
      context: ..
      dockerfile: Dockerfile
    image: custom-jenkins:latest
    ports:
      - "8090:8080"
      - "50000:50000"
    volumes:
      - D:/programowanie/jenkins/jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock